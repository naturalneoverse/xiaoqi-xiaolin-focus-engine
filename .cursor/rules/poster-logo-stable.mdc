---
description: 真我时刻海报 Logo 像素处理与预览（真机已验证，勿随意简化）
globs: miniprogram/subpkg/poster/**/*,miniprogram/utils/posterRenderer.js
alwaysApply: false
---

# 海报 Logo 与预览稳定性

## 必须保留的结构

1. **页内辅助画布** `subpkg/poster/index.wxml` 中的 `#posterLogoWork`（`type="2d"`），与主 `#posterCanvas` 一起在 `onReady` 里 `createSelectorQuery` 取 `node`，并在 `drawPosterFrame` 选项中传入 **`logoProcessCanvas`**。
2. **`posterRenderer.js` 绘制顺序**：Logo 槽内先 `clip` + 铺与海报一致的纵向渐变，再 **`drawLogoViaPixelCanvas`（优先）** → 失败时再 `wx.createOffScreenCanvas` → 最后再 `destination-in` fallback。
3. **`tintLogoImageData`**：底图用与渐变一致的**不透明 RGB** 填实，避免依赖「真透明」像素（相册/系统图库与 `<image>` 对 Alpha 表现不一致）。BFS 从四边扩展；浅灰平 / 深灰平 / 近白阈勿随意删窄，否则常见素材会回退方框。

## 预览

- `preview-wrap` 不要用实心白底衬托整张海报图，否则易与 Logo 透明问题混淆；保持 **transparent** 或与页面协调的极淡底。

## 修改前请自测

改上述任一路径后，请在真机验证：**小程序预览、存相册、系统相册打开** 三处 Logo 边缘一致。
