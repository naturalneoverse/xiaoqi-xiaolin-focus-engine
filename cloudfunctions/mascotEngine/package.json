{
  "name": "mascotEngine",
  "version": "1.0.0",
  "description": "Mascot copy: adapter → rules → (future LLM) → post-filter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "ISC",
  "dependencies": {
    "wx-server-sdk": "~2.4.0"
  }
}
