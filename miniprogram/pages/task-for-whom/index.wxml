<view class="page" style="{{_globalSafeAreaStyle}}">
  <view class="card">
    <view class="title-row">
      <view class="title-main">
        <image class="title-icon title-icon-who" src="/images/transparent background/who.png" mode="aspectFit" />
        <text class="title">为谁而做？</text>
      </view>
      <text class="step">2/3</text>
    </view>
    <view class="list">
      <view
        wx:for="{{options}}"
        wx:key="key"
        class="item {{selected === item.key ? 'item-selected' : ''}}"
        data-key="{{item.key}}"
        bindtap="chooseOption"
      >
        <view>
          <text class="item-title">{{item.key}}</text>
          <text class="item-desc">{{item.desc}}</text>
        </view>
        <text class="mark">{{selected === item.key ? "✓" : "○"}}</text>
      </view>
    </view>
  </view>

  <view class="tip">
    看见这件事服务的对象，你会更容易坚持并做对取舍。
  </view>

  <view class="actions">
    <button class="btn btn-light" bindtap="goPrev">上一步</button>
    <button class="btn" bindtap="goNext">下一页</button>
  </view>

  <view class="progress">
    <view class="dot"></view>
    <view class="dot dot-active"></view>
    <view class="dot"></view>
  </view>
</view>
