<view class="page" style="{{_globalSafeAreaStyle}}">
  <view class="top-nav">
    <view class="nav-btn tap-target" bindtap="goBack">
      <image class="nav-icon" src="/images/transparent background/close.png" mode="aspectFit" binderror="onImageError" />
    </view>
    <text class="nav-title">真我时刻海报</text>
    <view class="nav-space"></view>
  </view>

  <view wx:if="{{generating}}" class="loading-wrap">
    <text class="loading-text">正在生成...</text>
  </view>

  <view wx:if="{{previewSrc}}" class="preview-wrap">
    <image class="preview-img" src="{{previewSrc}}" mode="widthFix" show-menu-by-longpress />
  </view>

  <canvas
    type="2d"
    id="posterCanvas"
    class="poster-canvas-offscreen"
    style="width: 1080px; height: 1920px; background: transparent"
  ></canvas>
  <!-- 与主画布同源的 type=2d，用于 Logo 像素处理；真机上 createOffScreenCanvas 常不可用或 getImageData 异常 -->
  <canvas type="2d" id="posterLogoWork" class="poster-logo-work"></canvas>

  <view class="toolbar grid">
    <button class="tool-cell" bindtap="onRegenerate" disabled="{{generating}}">换一句</button>
    <button class="tool-cell" bindtap="onSaveAlbum" disabled="{{generating}}">存相册</button>
    <button class="tool-cell tool-primary" open-type="share" disabled="{{generating}}">发朋友</button>
    <button class="tool-cell" bindtap="onShareMoment" disabled="{{generating}}">朋友圈</button>
  </view>
</view>
